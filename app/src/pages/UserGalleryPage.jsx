/**
 * User Gallery Page: placeholder that renders as an Outlet in MainPage.
 * The actual user grid is rendered by MainPage when location.pathname === '/main/users'.
 */
export default function UserGalleryPage() {
  return null;
}
